<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Cor de fundo -->
    <solid android:color="#FFFFFF" />

    <!-- Borda -->
    <stroke
        android:width="1dp"
        android:color="#300F172A" />

    <!-- Borda Arredondada -->
    <corners android:radius="8dp" />

    <!-- Espaçamento interno -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="8dp"
        android:bottom="8dp"/>

</shape>
