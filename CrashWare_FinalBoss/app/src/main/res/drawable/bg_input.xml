<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Cor de fundo -->
    <solid android:color="#112233"/>

    <!-- Borda -->
    <stroke
        android:width="1dp"
        android:color="#3A4A5A"/>

    <!-- Cantos arredondados -->
    <corners android:radius="10dp"/>

    <!-- Espaçamento interno -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="8dp"
        android:bottom="8dp"/>

</shape>